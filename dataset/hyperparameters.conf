[DEFAULT]
# Hyperparameters for simple_language_model_shakespeare

epochs=2

# Model
model_type=transformer
n_layer=4
n_head=8
n_embd=128
dropout=0.1

# Data / tokenizer
dataset_path=dataset/input.txt
tokenizer=char
vocab_size=65
block_size=64

# Optimization / training
batch_size=64
learning_rate=3e-4
weight_decay=0.1
betas=0.9,0.95
grad_clip=1.0
max_iters=200000
lr_decay=true
warmup_iters=500
grad_clip=1.0

# Evaluation & checkpoints
eval_interval=1000
save_interval=5000
out_dir=out

# Misc
seed=1337
device=mps
# `compile_model` and `precision` removed — not required by the notebook

# Learning rate scheduler (optional)
# scheduler = none | step | cosine | plateau
# Example: enable cosine decay with linear warmup
scheduler=cosine
# Use per-batch stepping because warmup is defined in steps
scheduler_step_per=batch
# Number of warmup steps (linear increase from 0 -> lr)
scheduler_warmup_steps=500
# Total T_max is computed automatically from epochs*steps_per_epoch when using warmup
# fallback options for pure cosine or step LR
scheduler_T_max=50
scheduler_step_size=10
scheduler_gamma=0.1
scheduler_patience=5